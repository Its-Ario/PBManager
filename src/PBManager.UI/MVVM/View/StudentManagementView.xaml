<UserControl x:Class="PBManager.UI.MVVM.View.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PBManager.UI.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="15" Margin="0,0,10,0">
            <StackPanel>
                <TextBox x:Name="SearchBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Tag="جستجو"
                         Margin="0,0,0,10"
                         Style="{StaticResource ModernTextBox}"
                         VerticalContentAlignment="Center"
                         FlowDirection="RightToLeft"/>
                <DataGrid x:Name="dataGrid"
                          ItemsSource="{Binding FilteredStudents}"
                          SelectedItem="{Binding SelectedStudent}"
                          Style="{StaticResource ModernDataGrid}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          MaxHeight="400"
                          FlowDirection="RightToLeft"
                          AutoGenerateColumns="False"
                          CanUserSortColumns="True"
                          Sorting="DataGrid_Sorting"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام" Binding="{Binding FirstName}" SortMemberPath="FirstName" Width="*"/>
                        <DataGridTextColumn Header="نام خانوادگی" Binding="{Binding LastName}" SortMemberPath="LastName" Width="*"/>
                        <DataGridTextColumn Header="کلاس" Binding="{Binding Class.Name}" SortMemberPath="Class.Name" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="15" 
                Margin="10,0,0,0" Grid.Column="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=dataGrid}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentControl Content="{Binding DetailVM}" />
        </Border>

        <TextBlock Text="یک دانش آموز را انتخاب کنید."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="White"
                   FlowDirection="RightToLeft"
                   Grid.Column="1">
            <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=dataGrid}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="15" 
                Margin="10,0,0,0" Grid.Column="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=dataGrid}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentControl Content="{Binding DetailVM}" />
        </Border>
    </Grid>
</UserControl>
