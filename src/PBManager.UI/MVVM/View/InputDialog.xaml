<Window x:Class="PBManager.UI.MVVM.View.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="180" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#272537">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblQuestion" Grid.Row="0" Margin="0,0,0,5"
                   Foreground="White"/>

        <Border CornerRadius="10" Grid.Row="1"
                Background="#22202F">
            <TextBox x:Name="txtAnswer"
                     Background="Transparent"
                     VerticalContentAlignment="Center"
                     Foreground="White"
                     BorderThickness="0"
                     FlowDirection="RightToLeft"/>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnDialogOk" Content="تایید" IsDefault="True" MinWidth="60" Margin="0,0,10,0" Click="btnDialogOk_Click"
                    Style="{StaticResource ModernButtonStyle}"
                    Background="#44aa44"/>
            <Button Content="کنسل" IsCancel="True" MinWidth="60"
                    Height="20"
                    Background="Crimson"
                    Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>