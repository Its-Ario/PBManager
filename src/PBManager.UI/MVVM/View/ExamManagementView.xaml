<UserControl x:Class="PBManager.UI.MVVM.View.ExamManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PBManager.UI.MVVM.View"
             xmlns:converter="clr-namespace:PBManager.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converter:DateConverter x:Key="DateConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="15" Margin="0,0,10,0" Grid.Column="0">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="جستجو"
                             Margin="0,0,5,0"
                             Style="{StaticResource ModernTextBox}"
                             VerticalContentAlignment="Center"
                             FlowDirection="RightToLeft"
                             Grid.Column="0"/>

                    <Button Content="+"
                            Width="30"
                            Height="30"
                            Margin="0,0,0,0"
                            Style="{StaticResource ModernButtonStyle}"
                            Background="{StaticResource SubmitGreenBrush}"
                            Command="{Binding AddNewExamCommand}"
                            Grid.Column="1"/>
                </Grid>
                
                <DataGrid x:Name="dataGrid"
                          ItemsSource="{Binding FilteredExams}"
                          SelectedItem="{Binding SelectedExam}"
                          Style="{StaticResource ModernDataGrid}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          MaxHeight="350"
                          FlowDirection="RightToLeft"
                          IsReadOnly="True" 
                          AutoGenerateColumns="False"
                          CanUserSortColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام" 
                            Binding="{Binding Name}" 
                            Width="2*"/>

                        <DataGridTextColumn Header="تاریخ" 
                            Binding="{Binding Date, Converter={StaticResource DateConverter}}" 
                            Width="*"/>

                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" FlowDirection="RightToLeft">
            <TextBlock Text="یک آزمون را انتخاب کنید."
                       Foreground="White"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>

            <Button Content="+ آزمون جدید"
                    Style="{StaticResource ModernButtonStyle}"
                    Background="{StaticResource SubmitGreenBrush}"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Center"
                    Height="30" Width="100"
                    Margin="0,0,0,20"
                    Command="{Binding AddNewExamCommand}"/>
        <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=dataGrid}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
        </Grid>

        <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="15" 
                Margin="10,0,0,0" Grid.Column="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem, ElementName=dataGrid}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentControl Content="{Binding ExamVM}" />
        </Border>
    </Grid>
</UserControl>
